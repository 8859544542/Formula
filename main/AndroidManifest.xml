<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vishalkumar.mathematics">

    <application
        android:allowBackup="true"
        android:icon="@drawable/img"
        android:label="Learn with Fun"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashingActivity" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter></activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".AlgebraActivity" />
        <activity android:name=".TrignoActivity" />
        <activity android:name=".ButtonActivity">

        </activity>
        <activity android:name=".AreaVolumeActivity" />
        <activity android:name=".InversetrignoActivity" />
        <activity android:name=".DiffrentialActivity" />
        <activity android:name=".IntigrationActivity"></activity>
    </application>

</manifest>